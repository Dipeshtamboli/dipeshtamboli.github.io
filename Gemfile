source "https://rubygems.org"

# Matches the GitHub Pages build environment (Jekyll + supported plugins).
gem "github-pages", group: :jekyll_plugins

# Local-dev convenience (Ruby 3.x no longer bundles webrick).
gem "webrick", "~> 1.8"
